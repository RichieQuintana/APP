name: CI - Python App

on:
  push:
    branches: [ 'main' ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Login Docker Hub
      run: echo "${{ secrets.DOCKER_PASS }}" | docker login -u "${{ secrets.DOCKER_USER }}" --password-stdin

    - name: Build imagen Docker
      run: docker build -t dockeruser/app:${{ github.sha }} .

    - name: Push imagen
      run: docker push dockeruser/app:${{ github.sha }}

    - name: Actualizar repo INFRA
      run: |
        git clone https://github.com/TU_USUARIO/infra-proyecto-CI.git
        cd infra-proyecto-CI
        sed -i "s|image: .*|image: dockeruser/app:${{ github.sha }}|" k8s/deployment.yaml
        git config user.name 'github-actions'
        git config user.email 'actions@github.com'
        git add .
        git commit -m 'Actualizando deployment con nueva imagen'
        git push
