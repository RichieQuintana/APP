name: CI - Python App

on:
  push:
    branches: [ 'main' ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Login Docker Hub
      run: echo dckr_pat_7XF9-PunYL6e5Wf9uHF6DvkPrCg | docker login -u richieq --password-stdin

    - name: Build imagen Docker
      run: docker build -t richiequintana/app:${{ github.sha }} .

    - name: Push imagen
      run: docker push richiequintana/app:${{ github.sha }}

    - name: Actualizar repo INFRA
      run: |
        git clone https://github.com/RichieQuintana/infra-proyecto-CI.git
        cd infra-proyecto-CI
        sed -i "s|image: .*|image: richiequintana/app:${{ github.sha }}|" k8s/deployment.yaml
        git config user.name 'github-actions'
        git config user.email 'actions@github.com'
        git add .
        git commit -m 'Actualizando deployment con nueva imagen'
        git push
